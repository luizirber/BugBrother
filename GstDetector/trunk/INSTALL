This software uses autogen.sh to manage the autoconf tools. To compile
just type:

$ ./autogen.sh --prefix=/usr
$ make
$ make install

Plugins should be detected by gst-inspect now, and therefore be used by
GStreamer.
